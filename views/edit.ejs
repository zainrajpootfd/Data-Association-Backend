<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end">
        <a
          class="bg-red-500 px-3 py-2 rounded-md inline-block mb-2"
          href="/logout"
          >Logout</a
        >
      </div>

      <h5 class="mb-5 text-xl">Edit Your post</h5>
      <form action="/update/<%= post._id%>" method="post">
        <textarea
          class="w-1/3 outline-none py-3 px-2 resize-none bg-transparent border-2 bg-zinc-800 rounded-md block"
          placeholder="What's on your mind?"
          name="content"
        >
        <%= post.content%></textarea
        >
        <input
          class="w-40 px-3 py-2 bg-yellow-500 text-black rounded-md block mt-2"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
