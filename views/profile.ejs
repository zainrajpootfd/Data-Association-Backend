<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
      <div class="w-full flex justify-end gap-4">
        <a
          class="bg-zinc-800 px-3 py-2 rounded-md inline-block mb-2"
          href="/profile/upload"
          >Upload Profile Picture</a
        >
        <a
          class="bg-red-500 px-3 py-2 rounded-md inline-block mb-2"
          href="/logout"
          >Logout</a
        >
      </div>
      <div class="flex gap-3">
        <div class="w-10 h-10 rounded-md overflow-hidden">
          <img
            class="w-full h-full object-cover"
            src="/images/uploads/<%= user.profilepic %>"
            alt=""
          />
        </div>
        <h3 class="text-3xl">Hello! <%= user.name%>👋</h3>
      </div>
      <h5 class="mb-5 mt-3">you can create a new post</h5>
      <form action="/post" method="post">
        <textarea
          class="w-1/3 outline-none p-3 resize-none bg-transparent border-2 bg-zinc-800 rounded-md block"
          placeholder="What's on your mind?"
          name="content"
        ></textarea>
        <input
          class="w-40 px-3 py-2 bg-blue-500 rounded-md block mt-2"
          type="submit"
          value="Create New Post"
        />
      </form>
      <div class="posts mt-10">
        <h3 class="text-zinc-400">Your Posts.</h3>
        <div class="postcontainer mt-5">
          <% user.posts.reverse().forEach(function(post){%>

          <div
            class="post p-4 mt-4 w-1/3 bg-zinc-900 border-2 border-zinc-700 rounded-md"
          >
            <h4 class="text-blue-500 mb-2">@<%= user.username%></h4>
            <p class="text-sm tracking-tight"><%= post.content%></p>
            <small class="mt-4 inline-block"
              ><%= post.likes.length%> Likes</small
            >
            <div class="btns flex gap-4 mt-4">
              <a class="text-blue-500" href="/like/<%=post._id%>">
                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike" %>
              </a>
              <a class="text-zinc-600" href="/edit/<%=post._id%>">edit</a>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
